import TextGenerator from "./TextGenerator";
import Keywords from "./Keywords";

const Profile = () => {
    return (
        <div>
            ProfileInfo
            <TextGenerator />
            <Keywords />
        </div>
    );
};

export default Profile;
